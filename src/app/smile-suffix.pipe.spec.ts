import { SmileSuffixPipe } from './smile-suffix.pipe';

describe('SmileSuffixPipe', () => {
  it('create an instance', () => {
    const pipe = new SmileSuffixPipe();
    expect(pipe).toBeTruthy();
  });
});
